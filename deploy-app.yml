---
- name: Deploy PHP_Crud app
  hosts: "{{ target_group | default('staging') }}"
  become: true
  vars:
    artifact_path: "{{ artifact_path | default('') }}"
    mysql_app_password: "{{ mysql_app_password | default('app_pass') }}"
  roles:
    - apache_php
    - mysql_config
    - deploy_app
...